apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: yousirs-ingress
    annotations:
      nginix.ingress.kubernetes.io/use-regex: "true"
spec:
    rules:
    - host: localhost
      http:
        paths:
        - path: /
          backend:
            serviceName: yousirs-app
            servicePort: 5000
        - path: /users
          backend:
            serviceName: yousirs-app
            servicePort: 5000
        - path: /users/.*
          backend:
            serviceName: yousirs-app
            servicePort: 5000
        - path: /groups
          backend:
            serviceName: yousirs-app
            servicePort: 5000
        - path: /groups/.*
          backend:
            serviceName: yousirs-app
            servicePort: 5000
        - path: /easter/eggs
          backend:
            serviceName: yousirs-app
            servicePort: 5000
        - path: /easter/eggs/snap
          backend:
            serviceName: yousirs-app
            servicePort: 5000
